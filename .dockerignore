.git/
venv/
.venv/
__pycache__/
*.pyc
*.log
runs/
.env
node_modules/
# NOTE: model weights (*.pt) and *.mp4 assets are intentionally baked into the
# image for Cloud Run (no runtime Google Drive download / quota risk).
